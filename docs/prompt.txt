The current application is not performing well for the intended use case of chabot. about the application's current behaiviour it's issues, bugs and all the problems are mentioned in the RAG_PIPELINE_PRODUCTION_GAP_REPORT.md file. But as we need to change the embedding i have deleted the db so we fix the all the issues end to end in the application. Now read the given report and we need to fix all the issues it also suggest some of the solutions. An additional task is we have done setup of simple fronend and server as well but the RAG system is not integrated in that. as of now the scripts folder have basic data preparation scripts of python and its task is over if the report suggest any data enhancement so you can use the files of data and change data files directly carefully, the FY_CUT_OFF__Placement_data__Fee__Hostel_2025_-_26.xlsx file has multiple sheets and all the data we using which is converted to this json and jsonl data files so based on these files you can enhance the data carefully. The server folder is fastapi server in that a core folder is present where i want to place the core RAG system files and if required other files can be put in server folder and creating its subfolders, in core folder there is generator folder with a python script (whic i moved so need to change path) whichi is used to create vectorization of the data. In my POV the insufficient results even though the data is indirectly present in vector db is because of lack of semantic analysis and context of data, also the data is structured as questions and answer format it can also be root cause but i am not sure about this one, We are also not using graph rag and single node without analysis reports and response generation. we want to query multiple data analyze it and then generate response not just comparing characters. At end of the day we wwant a faultless and accurecy enriched RAG system which will easily handle issues mentioned in the RAG_PIPELINE_PRODUCTION_GAP_REPORT.md 
 At end create a .md file report reporting all changes you made what you changed etc.